# Claude — Sonata

Follow root `AGENTS.md` and `.agents/rules/project-manager-guide.md`.

Project tracking is in **Notion** (SONA Tickets), not Linear. Use Notion MCP.

Feature PRs target **`testing`**. Test Requests only if the user confirms —
use the `test-request` skill.
