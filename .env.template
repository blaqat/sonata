# GOOGLE_AI=
GOOGLE_AI=
OPEN_AI2=
OPEN_AI=
MISTRAL_AI=
ANTHROPIC_AI=
X_AI=
PPLX_AI=
BOT_APP_ID=
BOT_PUB_KEY=
# Discord bot identity — one token per Discord application / process.
# Staging: use a second Discord app + separate Railway service (see docs/staging-bot.md).
# Do not share BOT_TOKEN or Beacon storage between prod and staging.
BOT_TOKEN=

TENOR_G=
KLIPY=
OPEN_ASSIST=

WEATHER=
GIPHY=
SC=

SEARCH_ID=
SEARCH_KEY=

DC_TEST_LC_VC=
DC_TEST_LC_TLVC=
DC_TEST_LC_CHAT=

GOD=

# Cursor Cloud Agents (shared org API key — never put this in sonata.config.json)
CURSOR_API_KEY=

# Beacon — Railway: mount path for persistent files (default /beacon if unset).
# Local/dev: ignored; data lives under the plugin dir (beacon-mainland).
# Staging must use a different volume/path than prod (docs/staging-bot.md).
# BEACON_RAILWAY_PATH=/beacon
BEACON_RAILWAY_PATH=
# Staging: use a different encryption key than prod when encryption is enabled.
BEACON_ENCRYPTION_KEY=
TERM_WEB_ENABLED=
TERM_WEB_HOST=
TERM_WEB_PATH=
TERM_WEB_PORT=
TERM_WEB_SECRET=

# Optional path to config JSON. Staging should point at its own file/copy.
SONATA_CONFIG=
